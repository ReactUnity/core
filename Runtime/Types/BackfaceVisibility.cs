namespace ReactUnity.Types
{
    /// <summary>
    /// The <c>backface-visibility</c> property: whether an element still draws once a rotation has
    /// turned its back to the viewer.
    /// </summary>
    public enum BackfaceVisibility
    {
        Visible = 0,
        Hidden = 1,
    }
}
