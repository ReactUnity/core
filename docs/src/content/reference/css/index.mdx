---
title: CSS Properties
---

ReactUnity allows you to style elements using CSS. The CSS properties works the same as in browsers unless specified otherwise.

<Sandpack>

```js
export default function App() {
  return <>
    <view />
  </>;
};
```

```css active
:root {
  justify-content: space-around;
  text-align: center;
  background-color: #a9b2ba;
}

view {
  align-self: center;
  width: 100px;
  height: 100px;
  background-color: coral;
  cursor: pointer;
}

view:hover {
  background-color: blue;
}
```

</Sandpack>

### CSS Properties {/*css-properties*/}

List of supported CSS properties in alphabetical order:

- `align-content`
- `align-items`
- `align-self`
- [`animation-delay`](/reference/animations/animation)
- [`animation-direction`](/reference/animations/animation)
- [`animation-duration`](/reference/animations/animation)
- [`animation-fill-mode`](/reference/animations/animation)
- [`animation-iteration-count`](/reference/animations/animation)
- [`animation-name`](/reference/animations/animation)
- [`animation-play-state`](/reference/animations/animation)
- [`animation-range-end`](/reference/animations/scroll-timelines)
- [`animation-range-start`](/reference/animations/scroll-timelines)
- [`animation-timeline`](/reference/animations/scroll-timelines)
- [`animation-timing-function`](/reference/animations/animation)
- [`appearance`](/reference/css/appearance)
- `aspect-ratio`
- [`audio-clip`](/reference/animations/audio)
- [`audio-delay`](/reference/animations/audio)
- [`audio-iteration-count`](/reference/animations/audio)
- [`audio-pitch`](/reference/animations/audio)
- [`audio-volume`](/reference/animations/audio)
- [`backdrop-filter`](/reference/css/backdrop-filter)
- [`background-blend-mode`](/reference/css/background-blend-mode)
- [`background-color`](/reference/css/background-color)
- [`background-image`](/reference/css/background-image)
- [`background-position-x`](/reference/css/background-image)
- [`background-position-y`](/reference/css/background-image)
- [`background-repeat-x`](/reference/css/background-image)
- [`background-repeat-y`](/reference/css/background-image)
- [`background-size`](/reference/css/background-image)
- [`border-block-end-width`](/reference/css/border-width)
- [`border-block-start-width`](/reference/css/border-width)
- [`border-bottom-color`](/reference/css/border-color)
- [`border-bottom-left-radius`](/reference/css/border-radius)
- [`border-bottom-right-radius`](/reference/css/border-radius)
- [`border-bottom-style`](/reference/css/border-style)
- [`border-bottom-width`](/reference/css/border-width)
- [`border-end-width`](/reference/css/border-width)
- [`border-image-outset`](/reference/css/border-image)
- [`border-image-repeat`](/reference/css/border-image)
- [`border-image-slice`](/reference/css/border-image)
- [`border-image-source`](/reference/css/border-image)
- [`border-image-width`](/reference/css/border-image)
- [`border-inline-end-width`](/reference/css/border-width)
- [`border-inline-start-width`](/reference/css/border-width)
- [`border-left-color`](/reference/css/border-color)
- [`border-left-style`](/reference/css/border-style)
- [`border-left-width`](/reference/css/border-width)
- [`border-right-color`](/reference/css/border-color)
- [`border-right-style`](/reference/css/border-style)
- [`border-right-width`](/reference/css/border-width)
- [`border-start-width`](/reference/css/border-width)
- [`border-top-color`](/reference/css/border-color)
- [`border-top-left-radius`](/reference/css/border-radius)
- [`border-top-right-radius`](/reference/css/border-radius)
- [`border-top-style`](/reference/css/border-style)
- [`border-top-width`](/reference/css/border-width)
- `bottom`
- [`box-shadow`](/reference/css/box-shadow)
- `box-sizing`
- `caret-color`
- [`clip-path`](/reference/css/clip-path)
- [`color`](/reference/css/color)
- [`color-scheme`](/reference/css/color-scheme)
- `column-gap`
- [`container-name`](/reference/rules/container-queries)
- [`container-type`](/reference/rules/container-queries)
- [`content`](/reference/css/content)
- [`cursor`](/reference/css/cursor)
- `direction`
- `display`
- `end`
- [`fill`](/reference/components/svg)
- [`filter`](/reference/css/filter)
- `flex-basis`
- `flex-direction`
- `flex-grow`
- `flex-shrink`
- `flex-wrap`
- [`font-family`](/reference/css/font-family)
- `font-size`
- `font-style`
- [`font-variant`](/reference/css/text-transform)
- [`font-variant-caps`](/reference/css/text-transform)
- `font-weight`
- `height`
- [`image-rendering`](/reference/css/image-rendering)
- `inset-block-end`
- `inset-block-start`
- `inset-inline-end`
- `inset-inline-start`
- [`isolation`](/reference/css/isolation)
- `justify-content`
- `left`
- `letter-spacing`
- [`line-clamp`](/reference/css/max-lines)
- `line-height`
- `margin-block-end`
- `margin-block-start`
- `margin-bottom`
- `margin-end`
- `margin-horizontal`
- `margin-inline-end`
- `margin-inline-start`
- `margin-left`
- `margin-right`
- `margin-start`
- `margin-top`
- `margin-vertical`
- [`mask-image`](/reference/css/mask-image)
- [`mask-mode`](/reference/css/mask-image)
- [`mask-position-x`](/reference/css/mask-image)
- [`mask-position-y`](/reference/css/mask-image)
- [`mask-repeat-x`](/reference/css/mask-image)
- [`mask-repeat-y`](/reference/css/mask-image)
- [`mask-size`](/reference/css/mask-image)
- [`mask-type`](/reference/css/mask-image)
- `max-height`
- [`max-lines`](/reference/css/max-lines)
- `max-width`
- `min-height`
- `min-width`
- [`mix-blend-mode`](/reference/css/mix-blend-mode)
- [`motion-delay`](/reference/animations/motion)
- [`motion-duration`](/reference/animations/motion)
- [`motion-timing-function`](/reference/animations/motion)
- [`navigation`](/reference/css/navigation)
- `object-fit`
- `object-position`
- [`opacity`](/reference/css/opacity)
- `order`
- [`outline-color`](/reference/css/outline)
- [`outline-offset`](/reference/css/outline)
- [`outline-style`](/reference/css/outline)
- [`outline-width`](/reference/css/outline)
- [`overflow-x`](/reference/css/overflow)
- [`overflow-y`](/reference/css/overflow)
- `padding-block-end`
- `padding-block-start`
- `padding-bottom`
- `padding-end`
- `padding-horizontal`
- `padding-inline-end`
- `padding-inline-start`
- `padding-left`
- `padding-right`
- `padding-start`
- `padding-top`
- `padding-vertical`
- [`pointer-events`](/reference/css/pointer-events)
- [`position`](/reference/css/position)
- `right`
- [`rotate`](/reference/css/rotate)
- `row-gap`
- [`scale`](/reference/css/scale)
- [`scroll-behavior`](/reference/css/scroll-behavior)
- [`scroll-snap-align`](/reference/css/scroll-snap)
- [`scroll-snap-type`](/reference/css/scroll-snap)
- [`scroll-timeline-axis`](/reference/animations/scroll-timelines)
- [`scroll-timeline-name`](/reference/animations/scroll-timelines)
- [`scrollbar-gutter`](/reference/components/scroll)
- [`sorting-layer`](/reference/css/sorting-layer)
- `start`
- [`state-duration`](/reference/animations/enter-leave)
- [`stroke`](/reference/components/svg)
- `text-align`
- [`text-decoration-color`](/reference/css#text-decoration)
- [`text-decoration-line`](/reference/css#text-decoration)
- `text-overflow`
- [`text-shadow`](/reference/css/text-shadow)
- `text-stroke-color`
- `text-stroke-width`
- [`text-transform`](/reference/css/text-transform)
- [`text-wrap`](/reference/css/white-space)
- [`timeline-scope`](/reference/animations/scroll-timelines)
- `top`
- [`transform-origin`](/reference/css/transform-origin)
- [`transition-delay`](/reference/animations/transition)
- [`transition-duration`](/reference/animations/transition)
- [`transition-play-state`](/reference/animations/transition)
- [`transition-property`](/reference/animations/transition)
- [`transition-timing-function`](/reference/animations/transition)
- [`translate`](/reference/css/translate)
- [`translate-z`](/reference/css/transform)
- [`vertical-align`](/reference/css/vertical-align)
- [`view-timeline-axis`](/reference/animations/scroll-timelines)
- [`view-timeline-inset`](/reference/animations/scroll-timelines)
- [`view-timeline-name`](/reference/animations/scroll-timelines)
- [`visibility`](/reference/css/visibility)
- [`white-space`](/reference/css/white-space)
- `width`
- `word-spacing`
- [`z-index`](/reference/css/z-index)

### CSS Shorthands {/*css-shorthands*/}

List of supported CSS shorthands in alphabetical order:

- `all`
- [`animation`](/reference/animations/animation)
- [`animation-range`](/reference/animations/scroll-timelines)
- [`audio`](/reference/animations/audio)
- [`background`](/reference/css/background-image)
- [`background-position`](/reference/css/background-image)
- [`background-repeat`](/reference/css/background-image)
- `border`
- [`border-block-width`](/reference/css/border-width)
- `border-bottom`
- [`border-color`](/reference/css/border-color)
- [`border-image`](/reference/css/border-image)
- [`border-inline-width`](/reference/css/border-width)
- `border-left`
- [`border-radius`](/reference/css/border-radius)
- `border-right`
- [`border-style`](/reference/css/border-style)
- `border-top`
- [`border-width`](/reference/css/border-width)
- [`container`](/reference/rules/container-queries)
- `flex`
- `flex-flow`
- `font`
- `gap`
- `inset`
- `inset-block`
- `inset-inline`
- `margin`
- `margin-block`
- `margin-inline`
- [`mask`](/reference/css/mask-image)
- [`mask-position`](/reference/css/mask-image)
- [`mask-repeat`](/reference/css/mask-image)
- [`motion`](/reference/animations/motion)
- [`outline`](/reference/css/outline)
- [`overflow`](/reference/css/overflow)
- `padding`
- `padding-block`
- `padding-inline`
- `place-content`
- `place-items`
- `place-self`
- [`scroll-timeline`](/reference/animations/scroll-timelines)
- [`scrollbar-color`](/reference/components/scroll)
- [`scrollbar-width`](/reference/components/scroll)
- [`text-decoration`](/reference/css#text-decoration)
- `text-stroke`
- [`transform`](/reference/css/transform)
- [`transition`](/reference/animations/transition)
- [`view-timeline`](/reference/animations/scroll-timelines)

### Math functions {/*math-functions*/}

`calc()`, `min()`, `max()` and `clamp()` are joined by `round()` with its `nearest`, `up`, `down` and `to-zero` strategies, `mod()`, `rem()`, `abs()`, `sign()`, `sin()`, `cos()`, `tan()`, `asin()`, `acos()`, `atan()`, `atan2()`, `pow()`, `sqrt()`, `hypot()`, `log()` and `exp()`. They nest freely inside one another and inside `calc()`, and take `var()` operands. The constants `pi`, `e`, `infinity`, `-infinity` and `NaN` are known.

```css
view {
  animation-duration: round(up, 1250ms, 1s);
  opacity: calc(0.5 + 0.5 * sin(var(--phase)));
  flex-grow: calc(pow(2, var(--level)) - 1);
}
```

`place-content`, `place-items` and `place-self` set the align and justify pair in one declaration. Yoga has no `justify-items` or `justify-self`, so the last two set their align property and only check that the second value is a keyword. The web spellings `start`, `end`, `self-start`, `self-end`, `left`, `right` and `normal` work on the align and justify longhands too.

### Display and the flex defaults {/*display-and-the-flex-defaults*/}

Every element lays out with flexbox, and `display` picks the defaults a browser would give it. An
element with no `display`, or with `display: block`, stacks its children the way a block does, and
`display: flex` lays them out in a row. Either way `flex-direction`, `gap`, `align-items` and the rest
work on it. `inline-block` and `flow-root` read as `block`, `inline-flex` as `flex`, and `none` and
`contents` do what they do on the web.

`flex-shrink` defaults to 1 as on the web, so an item gives up space before it overflows, and the
`flex` shorthand fills in the web's values: `flex: 1` is `1 1 0%`, `flex: auto` is `1 1 auto` and
`flex: none` is `0 0 auto`.

### Logical properties {/*logical-properties*/}

The inline axis is the one `direction` turns around, and the block axis is always vertical --
`writing-mode` does not exist here, so `block-start` is the top and `block-end` the bottom whatever
else is going on.

```css
view {
  padding-inline: 4px 12px;
  padding-block-start: 8px;
}
```

`direction` is inherited, so setting it once turns the inline axis around for a whole subtree:

```css
#panel {
  direction: rtl;
}
```

Which physical side each inline value lands on is decided during layout, by Yoga, from the direction
the element ends up with -- an element does not need a `direction` of its own to follow one set on an
ancestor. An inline value takes precedence over the physical property covering the same edge,
whichever order the two are declared in; CSS would let the later declaration win, and it is the one
place the two disagree, so do not set both on one edge.

Under UIToolkit the inline axis is always the physical one: UIElements has no start and end edge to
resolve against, and `direction` does nothing there.

### Text decoration {/*text-decoration*/}

`text-decoration` is a shorthand for the line and its colour. TextMeshPro draws the line from the
font asset's own metrics and has only a solid form, so the style and thickness components are read
and then dropped -- the point being that they do not take the line down with them:

```css
a { text-decoration: underline red; }
a:hover { text-decoration: underline dotted 2px currentcolor; }
del { text-decoration: line-through #888; }
```

`underline`, `line-through` and `none` are the lines, and they combine. `overline` and `blink` are
not accepted at all, because nothing can draw them, so a declaration asking for one is dropped.
`text-decoration-style` and `text-decoration-thickness` do not exist as longhands for the same
reason -- there is nothing for them to set.

`text-decoration-color` defaults to `currentcolor` and, unlike on the web, is inherited: the line
itself rides on `font-style`, which inherits, so a non-inherited colour would draw nested text's
underline in the wrong one. A colour that differs from `color` is applied through TextMeshPro's rich
text tags, which means it needs `richText` on -- the default for a `<text>` -- and is skipped when it
is off.

`font-style` is one property carrying italic, bold and the letter casing as well as the two lines, so
the shorthand sets only the components you name: `text-decoration: red` leaves the line as it is,
where CSS would reset it to `none`.

### Custom states {/*custom-states*/}

Any pseudo-class ReactUnity does not know is a custom state: `view:busy` matches while a state named
`busy` is started on the element, from C# through `component.StateStyles.StartState("busy")` and
`EndState`. `:state(busy)` is the standard spelling of the same thing, and the one to prefer, since a
bare unknown name is warned about once in case it was a typo. Both take the specificity of a
pseudo-class.

```css
.slot:state(busy) {
  opacity: 0.5;
}
```
