fund=false
audit=false

# pnpm's equivalent of npm's legacy-peer-deps. This repo has real peer conflicts
# (react 18 in docs vs react 19 everywhere else), so relax rather than fail.
strict-peer-dependencies=false

# Loads tsx so .ts config/scripts are runnable from lifecycle scripts.
node-options="--import tsx"
