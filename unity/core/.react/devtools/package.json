{
  "name": "@reactunity/devtools",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "start": "react-unity-scripts start",
    "build": "react-unity-scripts build"
  },
  "dependencies": {
    "@reactunity/renderer": "workspace:*",
    "clsx": "^2.1.1",
    "react": "^19.2.8"
  },
  "//typescript": "Held at 6.x while the root and packages/create are on 7: this app builds with react-unity-scripts, which runs fork-ts-checker against whatever `resolve.sync('typescript')` finds from this directory, and 7 has no CJS entry to find. See packages/scripts.",
  "devDependencies": {
    "@reactunity/scripts": "workspace:*",
    "typescript": "~6.0.3"
  }
}
