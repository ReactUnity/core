---
packages:
  "upm:com.reactunity.core": minor
---

`font-family` takes a list, which is how CJK and emoji fallback is expressed, and several `@font-face` rules
may name one family so `font-weight` and `font-style` pick between its faces.
