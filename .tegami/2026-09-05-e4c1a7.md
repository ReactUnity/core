---
packages:
  npm:@reactunity/renderer:
    type: minor
  upm:com.reactunity.core:
    type: minor
---

Container queries work — `container-type`, `container-name`, `@container` with size and `style()` conditions,
and the `cq*` units.
