---
packages:
  "npm:@reactunity/renderer": minor
---

`@reactunity/renderer/tailwind.css` replaces Tailwind's Preflight with a reset written for what ReactUnity
renders, and `@reactunity/renderer/vite` aliases `@import "tailwindcss"` to it.
