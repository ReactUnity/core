---
packages:
  npm:@reactunity/renderer:
    type: minor
  upm:com.reactunity.core:
    type: minor
---

### `@starting-style`, `light-dark()`, `color-scheme`, `place-*`, the math functions, `:nth-child(of S)`, `attr()`, `linear()` and boolean hover queries

**`@starting-style`** is the style an element has on the frame it appears, and the state a
`transition` starts from. It used to collapse into the enclosing rule, so
`@starting-style { opacity: 0 }` made the element invisible for good. Each rule in a block is now
applied through `:enter`, which is the one visible deviation: `:enter` adds pseudo-class specificity
where the web adds none.

**`light-dark(<light>, <dark>)`** picks one of two values wherever a colour goes, and the new
`color-scheme` property (`normal`, `light`, `dark`, `light dark`; inherited) decides. A single
scheme settles it for the subtree, anything else defers to the `prefers-color-scheme` media feature,
and a change to that feature restyles the tree. `color-scheme` on the root element also seeds
`prefers-color-scheme`, unless a value was set through `MediaProvider.SetValue`.
`DefaultMediaProvider.SeedValue` is the public shape of that.

**`place-content`, `place-items` and `place-self`** set the align and justify pair in one
declaration. Yoga has no `justify-items` or `justify-self`, so the last two set their align property
and only check that a second value is a keyword. The align and justify longhands also take the web
spellings `start`, `end`, `self-start`, `self-end`, `left`, `right` and `normal`.

**The math functions** beyond `min()`, `max()` and `clamp()`: `round()` with `nearest`, `up`, `down`
and `to-zero`, `mod()`, `rem()`, `abs()`, `sign()`, `sin()`, `cos()`, `tan()`, `asin()`, `acos()`,
`atan()`, `atan2()`, `pow()`, `sqrt()`, `hypot()`, `log()` and `exp()`, plus the constants `pi`,
`e`, `infinity`, `-infinity` and `NaN`. Operands are `calc()` expressions, so they nest in one
another and take `var()`. The property's own converter reads the operands, so `round(1250ms, 1s)` is
a duration and `round(1s, 1)` mixes the two and is invalid. Trigonometric arguments are angles or
radians and the inverse functions return degrees. `calc()` with a leading sign, `calc(-1 * 10px)`,
used to fail to parse and now works.

**`:nth-child(An+B of S)`** and `:nth-last-child(An+B of S)` count only the siblings matching `S`, a
selector list of compound selectors, and add the specificity of the most specific branch.

**`attr()`** reads a `data-*` prop into any property: `content: attr(data-label)`, with the prefix
optional, a fallback as the second argument, and a unit after the name (`attr(data-size px)`) or a
type for a typed value. A pseudo element reads the element it belongs to.

**`linear()`** joins `steps()` and `cubic-bezier()` as an easing: `linear(0, 0.25 40%, 1)`, with up
to two positions per stop to hold a value, and extrapolation past the ends.

**The `hover`, `any-hover`, `pointer` and `any-pointer` media features are always defined**, so the
boolean forms `(hover)` and `(pointer)` are the standard way to gate hover-only styling; a platform
whose real value was the provider's default used to leave them undefined. Two provider bugs went
with it: a mobile platform registered the `console` media type instead of `mobile`, and the legacy
Input path listed a touch screen as a fine pointer.

`RenderStyle` gains `colorScheme`, `placeContent`, `placeItems` and `placeSelf`, and the align and
justify types take the new spellings.
