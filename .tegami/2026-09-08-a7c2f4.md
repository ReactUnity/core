---
packages:
  upm:com.reactunity.core:
    type: minor
---

`position: sticky` and `position: fixed` are implemented for UGUI, and `scroll-state(stuck: ...)` reads a
stuck container. Under UIToolkit, `position: static` is in flow again rather than absolute.
