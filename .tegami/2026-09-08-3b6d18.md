---
packages:
  npm:@reactunity/renderer:
    type: minor
  npm:@reactunity/material:
    type: patch
  upm:com.reactunity.core:
    type: minor
---

`position: inset` is removed. Use `absolute`, which Yoga now resolves correctly against a containing block
that has padding.
