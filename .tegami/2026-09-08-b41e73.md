---
packages:
  npm:@reactunity/renderer:
    type: minor
  upm:com.reactunity.core:
    type: minor
---

### The generated Typescript models are current again

`packages/renderer/src/models/generated/*.ts` had not been regenerated since January 2025. All six
files are rewritten against a current Editor, which is a year of Unity API movement plus everything
ReactUnity has added since.
