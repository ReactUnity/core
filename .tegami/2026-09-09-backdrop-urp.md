---
packages:
  "upm:com.reactunity.core": patch
---

`backdrop-filter` blurs correctly under URP and measures its distances in screen pixels, and `scanlines()`
lands on rows again under both pipelines.
