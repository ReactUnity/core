---
packages:
  "npm:@reactunity/renderer": minor
  "upm:com.reactunity.core": minor
---

The CSS logical box properties work — `padding-inline`, `margin-block`, `inset-inline-start` and the rest —
so `direction` turns the inline axis around for a whole subtree and Tailwind's `px-*`, `ps-*` and `space-x-*`
utilities do something.
